@using MVCBierenApplication.Models
@model  List<Bier>

@{
    ViewBag.Title = "Index";
}

<h2>Alle Bieren</h2>
<ul>
    @foreach(var bier in Model)
    {
        <li> @Html.DisplayFor(m => bier.ID) @bier.Naam @Html.DisplayFor(m => bier.Alcohol) </li>
    }
</ul>

